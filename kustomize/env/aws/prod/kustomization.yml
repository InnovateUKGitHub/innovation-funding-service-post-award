commonAnnotations:
  deployedWith: kustomize
  environment: prod

bases:
  - ../aws-base

resources:
  - ../../../config-mgmt/env/aws/prod

patchesStrategicMerge:
  - extui-deployment.yml
  - acc-ui-route.yml
