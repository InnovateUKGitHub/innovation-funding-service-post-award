{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**"],
      "outputs": ["dist/**"],
      "dependsOn": ["@innovateuk/logger#typegen"]
    },
    "typegen": {
      "inputs": ["src/**"],
      "outputs": ["dist/**"]
    }
  }
}
