import { Meta, Story, Props, Canvas } from "@storybook/blocks";
import * as Stories from "./Hint.stories";

<Meta of={Stories} />

# Hint

This is a GDS Hint element. The `id` attribute should always be present to help link to the target element

<Canvas of={Stories.StandardHint} />
