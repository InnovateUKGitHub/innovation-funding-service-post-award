{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "IFS-PA: start:dev",
      "request": "launch",
      "command": "npm run start:dev",
      "cwd": "${workspaceFolder}",
      "skipFiles": ["<node_internals>/**", "${workspaceFolder}/node_modules/**/*.js", "${workspaceFolder}/**/*.js"]
    }
  ]
}
