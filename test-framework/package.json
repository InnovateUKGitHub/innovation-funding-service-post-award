{
  "name": "@innovateuk/acc-test-framework",
  "private": true,
  "workspaces": [
    "./cypress-test",
    "./project-factory",
    "./environment-manager",
    "./playwright-test"
  ],
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "cypress": "turbo cypress",
    "open": "turbo open",
    "open:dev": "TEST_SALESFORCE_SANDBOX=dev && turbo open",
    "open:bjssdev": "TEST_SALESFORCE_SANDBOX=dev && turbo open",
    "open:at": "TEST_SALESFORCE_SANDBOX=at && turbo open",
    "run": "turbo run",
    "run:dev": "TEST_SALESFORCE_SANDBOX=dev && turbo run",
    "run:bjssdev": "TEST_SALESFORCE_SANDBOX=dev && turbo run",
    "run:at": "TEST_SALESFORCE_SANDBOX=at && turbo run"
  },
  "dependencies": {
    "turbo": "^2.1.1"
  },
  "packageManager": "npm@10.5.2"
}
