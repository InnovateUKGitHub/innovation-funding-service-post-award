import { Meta, Story, Props, Canvas } from "@storybook/blocks";
import * as Stories from "./Button.stories";

<Meta of={Stories} />

# Submit Button

The name must be present and prefixed with `button-`.

<Canvas of={Stories.StandardSubmitButton} />
